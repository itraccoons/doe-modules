#!/bin/sh
# shellcheck disable=SC2039

set -ex

[ -d "/tmp/bazel" ] || mkdir /tmp/bazel
ln -s /tmp/bazel ~/.cache/bazel

cd ~/src/nomulus || exit

bazel	--output_base=/tmp/bazel/output \
	--batch build \
	--javacopt="-target 8 -source 8" \
	//java{,tests}/google/registry/...

set +x
