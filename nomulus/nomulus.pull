#!/bin/sh
# shellcheck disable=SC2164

set -ex

cd ~ 
if [ ! -d "src" ]; then
    mkdir src
fi

cd src
if [ ! -d "nomulus" ]; then
    git clone https://github.com/google/nomulus.git
else
    cd nomulus
    git pull
fi

set +x